# ClaudeGUI - 功能概览

## 核心功能

### 会话管理
- **侧边栏会话列表**：左侧显示所有后台 agent 会话，按状态分组（等待输入、工作中、已完成、空闲）
- **状态分组可折叠**：点击分组行任意位置可展开/收起，带箭头动画指示
- **新建会话**：输入任务描述 + 选择工作目录，执行 `claude --bg "任务"` 创建后台会话
- **右键菜单**：右键点击会话行可选择"停止会话"（`claude stop`）或"删除会话"（`claude rm`）

### 终端
- **概览终端**：默认显示 `claude agents` TUI 界面
- **会话终端**：点击侧边栏会话后打开独立终端，执行 `claude attach <id>` 进入会话
- **多终端切换**：每个会话有独立终端实例，切换时直接显示对应终端
- **拖拽文件**：从 Finder 拖拽文件到终端，自动格式化为 `@/path/to/file`

### 会话同步
- **自动轮询**：每 5 秒执行 `claude agents --json` 刷新会话列表
- **手动刷新**：按 `Cmd+R` 立即刷新

## 界面

### 侧边栏
- 语言切换按钮（中/英）
- 主题切换按钮（基础/暗色/亮色），点击循环切换，选择自动持久化
- "所有会话"按钮回到概览视图
- "新建会话"按钮创建后台会话
- 会话按状态分组显示，每行显示 shortId + 会话名
- 底部显示 agent 总数

### 布局
- 左侧侧边栏 + 右侧终端的分栏布局
- 侧边栏宽度可拖拽调节（180~300pt）

### 工作区挂载
- **挂载 VS Code 工作区**：点击侧边栏顶部的挂载按钮，选择 `.code-workspace` 文件，自动提取关联目录并通过 `--add-dir` 参数传递给新会话
- **取消挂载**：展开已挂载工作区，点击"取消挂载工作区"按钮
- **关联目录显示**：挂载后在工作区下方展示所有关联目录
- **配置变更提示**：重新挂载不同配置文件后，显示黄色提示提醒新建会话

## 快捷键
- `Cmd+R`：手动刷新会话列表
- `Cmd+1~9`：快速切换到第 N 个会话

## 主题切换
- 支持三种配色方案：Basic（基础暗黑）、Clear Dark（通透暗色）、Clear Light（明亮浅色）
- Clear Dark 配色取自 macOS Terminal 的 Clear Dark 方案，背景带半透明效果
- Clear Light 配色取自 macOS Terminal 的 Clear Light 方案，白色背景 + 深蓝灰文字
- 点击侧边栏主题按钮循环切换：基础 → 暗色 → 亮色
- 主题选择通过 UserDefaults 持久化，重启后保留
- 切换主题时，侧边栏、对话框、终端颜色同步更新

## 本地化
- 支持中文/英文切换，默认中文
- 所有 UI 文本均支持双语
- 主题按钮文字跟随语言切换（基础/暗色/亮色 ↔ Basic/Dark/Light）
